---
import Layout from '../layouts/Layout.astro';

let posts = [];
try {
  const { getCollection } = await import('astro:content');
  posts = (await getCollection('blog'))
    .filter(p => !p.data.draft)
    .sort((a, b) => new Date(b.data.date) - new Date(a.data.date));
} catch(e) { posts = []; }

function fmtDate(d) {
  if (!d) return '';
  return new Date(d).toLocaleDateString('pt-BR', { day: '2-digit', month: 'short', year: 'numeric' });
}

// Pega um post por categoria para o pool do hero
const seen = new Set();
const siloPosts = posts.filter(p => {
  const cat = p.data.category || 'Geral';
  if (seen.has(cat)) return false;
  seen.add(cat);
  return true;
});

// Demais posts para o grid inferior
const gridPosts = posts.filter(p => !siloPosts.includes(p)).slice(0, 6);
---

<Layout>

  {siloPosts.length > 0 ? (
    <section class="hero" id="heroSection">
      <div class="container">
        <div class="hero-grid">

          <!-- HERO PRINCIPAL: preenchido pelo JS com o post sorteado -->
          <article class="hero-main" id="heroMain" style="opacity:0; transition: opacity 0.4s ease;">
            <a id="heroImgLink" href="#" class="hero-main-img-wrap">
              <img id="heroImg" src="" alt="" class="hero-main-img" width="820" height="461" loading="eager" fetchpriority="high" decoding="sync" />
            </a>
            <span id="heroCat" class="hero-cat"></span>
            <h1 class="hero-title">
              <a id="heroTitleLink" href="#"></a>
            </h1>
            <p id="heroExcerpt" class="hero-excerpt"></p>
            <a id="heroBtn" href="#" class="btn-read-full">
              Ler análise completa
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
            </a>
          </article>

          <!-- SIDEBAR: preenchida pelo JS -->
          <aside class="hero-sidebar" id="heroSidebar">
            <div class="hero-sidebar-header">Outras análises</div>
            <div id="sidebarPosts"></div>
          </aside>

        </div>
      </div>
    </section>
  ) : (
    <section class="section section-white">
      <div class="container">
        <div class="empty-state">
          <h2>Em breve: reviews e guias de compra</h2>
          <p>Estamos preparando análises completas para você. Volte em breve!</p>
        </div>
      </div>
    </section>
  )}

  <!-- GRID inferior -->
  {gridPosts.length > 0 && (
    <section class="section section-gray">
      <div class="container">
        <div class="section-header">
          <div>
            <span class="section-label">Mais Categorias</span>
            <h2 class="section-title">Explore por assunto</h2>
          </div>
          <a href="/blog" class="section-link">
            Ver todos
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
          </a>
        </div>
        <div class="post-grid">
          {gridPosts.map(post => (
            <article class="post-card">
              <a href={`/blog/${post.slug}`} class="post-card-img-wrap">
                <img src={post.data.image || '/placeholder.webp'} alt={post.data.title} class="post-card-img" width="600" height="400" loading="lazy" />
              </a>
              <div class="post-card-body">
                <span class="post-card-cat">{post.data.category}</span>
                <h3 class="post-card-title"><a href={`/blog/${post.slug}`}>{post.data.title}</a></h3>
                {post.data.description && <p class="post-card-excerpt">{post.data.description}</p>}
              </div>
              <div class="post-card-footer">
                <time class="post-card-date" datetime={post.data.date?.toISOString?.() ?? ''}>{fmtDate(post.data.date)}</time>
                <a href={`/blog/${post.slug}`} class="post-card-read">
                  Ler review
                  <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
                </a>
              </div>
            </article>
          ))}
        </div>
      </div>
    </section>
  )}

  <!-- Pool de posts serializado para o JS -->
  <script type="application/json" id="postsPool">
    {JSON.stringify(siloPosts.map(p => ({
      slug: p.slug,
      title: p.data.title,
      description: p.data.description || '',
      category: p.data.category || '',
      image: p.data.image || '/placeholder.webp',
      url: `/blog/${p.slug}`,
    })))}
  </script>

  <script>
    const pool = JSON.parse(document.getElementById('postsPool').textContent);
    if (!pool || pool.length === 0) {
      document.getElementById('heroSection')?.remove();
    } else {
      // Sorteia hero aleatório a cada refresh
      const idx = Math.floor(Math.random() * pool.length);
      const hero = pool[idx];
      const sidebar = [...pool.slice(0, idx), ...pool.slice(idx + 1)].slice(0, 4);

      // Preenche hero
      const heroMain   = document.getElementById('heroMain');
      const imgLink    = document.getElementById('heroImgLink');
      const img        = document.getElementById('heroImg');
      const cat        = document.getElementById('heroCat');
      const titleLink  = document.getElementById('heroTitleLink');
      const excerpt    = document.getElementById('heroExcerpt');
      const btn        = document.getElementById('heroBtn');

      imgLink.href     = hero.url;
      img.src          = hero.image;
      img.alt          = hero.title;
      cat.textContent  = hero.category;
      titleLink.href   = hero.url;
      titleLink.textContent = hero.title;
      excerpt.textContent   = hero.description;
      btn.href         = hero.url;

      // Fade in suave
      requestAnimationFrame(() => { heroMain.style.opacity = '1'; });

      // Preenche sidebar
      const sidebarEl = document.getElementById('sidebarPosts');
      sidebar.forEach(post => {
        sidebarEl.insertAdjacentHTML('beforeend', `
          <a href="${post.url}" class="sidebar-post">
            <img src="${post.image}" alt="${post.title}" class="sidebar-post-img" loading="lazy" width="72" height="54" />
            <div>
              <span class="sidebar-post-cat">${post.category}</span>
              <span class="sidebar-post-title">${post.title}</span>
            </div>
          </a>
        `);
      });
    }
  </script>

</Layout>
